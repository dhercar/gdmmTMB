# Nice titles for summary tables

Nice titles for summary tables

## Usage

``` r
print_title(text, symb = "-", width = getOption("width"))
```

## Arguments

- text:

  Title to display

- symb:

  Symbol used above and below the title

- width:

  Size of the console. Defaults to current width.

## Value

A `character` string (invisibly returned)
