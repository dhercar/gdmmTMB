# Calculate AICc

Calculates the Akaike Information Criterion corrected for small sample
sizes (AICc).

## Usage

``` r
AICc(log_likelihood, n, k)
```

## Arguments

- log_likelihood:

  The log-likelihood of the model

- n:

  The number of observations

- k:

  The number of estimated parameters
